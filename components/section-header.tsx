export function SectionHeader({ eyebrow, title, light = false }: { eyebrow: string; title: string; light?: boolean }) { return <div><p className={`text-xs font-semibold uppercase tracking-[0.22em] ${light ? 'text-gold' : 'text-clay'}`}>{eyebrow}</p><h2 className={`mt-4 max-w-2xl font-display text-4xl leading-[1.05] tracking-[-0.04em] sm:text-5xl ${light ? 'text-ivory' : 'text-forest'}`}>{title}</h2></div>; }
